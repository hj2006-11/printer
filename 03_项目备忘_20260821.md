# 03 项目备忘（D3）

> 用途：摘抄项目关键信息，供 AI 与本人快速理解现状，避免重复读全量报告。本备忘为 D3 快照。

## 1. 项目一句话

基于 Golang + 无头 Chromium 的本地打印服务 MVP：浏览器打开本地页面 → HTTP 提交打印任务 → 服务渲染 PDF → 驱动默认打印机（无打印机则 PDF 兜底）→ 页面轮询状态。

## 2. 技术栈与运行方式

- 语言/框架：Golang（标准库 HTTP）+ 无头 Chromium 渲染 HTML→PDF
- 打印：Windows `powershell -Verb Print`，Linux `lp`；无实体打印机时 PDF 即产物
- 目录：`printsvc/`（`cmd/printsvc` 入口，`internal/{port,server,task,render,printer,static}`）
- 构建运行：
  ```bash
  cd printsvc && go build -o printsvc.exe ./cmd/printsvc && ./printsvc.exe
  # 浏览器打开 http://127.0.0.1:18210（或 port.txt 中端口）
  ```

## 3. 接口约定（v1.0，已冻结，方案 B：REST 资源化 + 按类型分组）

基路径 `/api/v1`，服务默认 `127.0.0.1:18210`，端口被占则 18210~18220 递增回退，实际端口写 `port.txt`。

| 方法/路径 | 作用 | 关键返回 |
| --- | --- | --- |
| `GET /api/v1/healthz` | 探活 | `{status:"ok", service, version}` |
| `POST /api/v1/tasks` | 提交任务 | 202 `{task_id, status:"pending", created_at}` |
| `GET /api/v1/tasks/{id}` | 查询任务 | 200 任务详情；404 `{error:{code,message}}` |

- **请求体按 `type` 分组**：`{"type":"ticket","data":{team, problem, pass_time?}}` 或 `{"type":"code","data":{lang?, source}}`
- **错误统一**：`{"error":{"code":..., "message":...}}`；code ∈ `INVALID_ARGUMENT`(400) / `NOT_FOUND`(404) / `METHOD_NOT_ALLOWED`(405)
- **时间**：`created_at`=RFC3339；`pass_time`=`YYYY-MM-DD HH:MM:SS`
- **task_id**：`T{4位序号}-{unix秒}`
- **状态机**：`pending → completed | failed`；异步失败体现在任务状态（RENDER_FAILED/PRINT_FAILED），不进同步响应码
- 旧路由 `/submit`、`/status?id=` 已废弃迁移（见接口文档第 3 节）

## 4. 数据模型（v1.0，已冻结）

- **Task**：`{task_id, type(ticket|code), status, message?, output_path?, created_at, data{team|problem|pass_time|lang|source}}`
- **Queue**：有缓冲 channel（容量10）+ `map[string]*Task` + RWMutex；单 worker 串行消费；`Submit` 生成 ID 入 map+channel，`Get` 读锁查询；不持久化，重启清空
- **PDF**：`{workdir}/output/{type}_{纳秒}.pdf`；临时 HTML 渲染后删除
- 完整文档：《数据模型_20260821.md》

## 5. 假实现与兜底边界（勿误认为已完成）

| 环节 | 现状 | 换真计划 |
| --- | --- | --- |
| 语法高亮 | HTML 转义 + CSS 模拟 | D4 接 chroma |
| 实体打印 | PDF 兜底（无实体打印机） | D7 实机验证 |
| Linux 渲染 | 仅 Windows 验证（本机无 WSL） | D6 交叉编译 + 远程验证 |
| 端口全占用边界 | 已实现回退，未实测占满 | D5 脚本验证 |

## 6. 待决问题

- 语法高亮库 chroma 拉取/离线 fallback（D4）
- Linux 演示环境定案（D6 前）
- 端口全占用行为（D5）

## 7. 已交付物（D3）

- `接口书面约定_20260821.md/.docx`、`数据模型_20260821.md/.docx`、`第3天报告_20260821.md/.docx`、`03_项目备忘_20260821.md`
- `printsvc/` 已按契约 v1.0 改造（REST /api/v1 路由）
