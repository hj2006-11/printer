# printsvc 代码应用技术与知识点解析（第 5 天）

> 本文面向希望读懂 `printsvc/` 源码的读者，按「模块 → 用到的技术 → 为什么这样设计」逐层解析。所有结论均对应当前代码（Go 1.26）。

## 1. 整体架构与分层

```
浏览器 ──HTTP──> server（HTTP API + 静态页）
                    │ 提交任务（POST /api/v1/tasks）
                    ▼
                task.Queue（channel 队列 + 状态机）
                    │ 单 worker goroutine 串行消费
                    ▼
                render.Engine（Chrome headless HTML→PDF）
                    ▼
                printer.Adapter（默认打印机 / PDF 兜底）
                    ▼
                output/*.pdf
```

- **包结构**：`cmd/printsvc`（入口）、`internal/port`、`internal/server`、`internal/task`、`internal/render`、`internal/printer`、`internal/static`。
- Go 的 `internal/` 目录约定：`internal` 下的包只能被同模块（`ccpc-printsvc`）内部导入，外部项目无法引用，是一种**编译期强制封装**。

## 2. 并发模型：channel + 单 worker goroutine

代码位置：`internal/task/task.go`

```go
type Queue struct {
    ch    chan *Task     // 任务队列，容量 10
    tasks map[string]*Task // 状态表
    mu    sync.RWMutex   // 保护 tasks 的读写
}

func (q *Queue) Start() { go q.worker() }  // 后台 worker

func (q *Queue) Submit(t *Task) string {
    q.mu.Lock(); q.counter++; t.ID = ...; q.tasks[t.ID] = t; q.mu.Unlock()
    q.ch <- t            // 入队（会阻塞直到 worker 取走）
    return t.ID
}

func (q *Queue) worker() {
    for t := range q.ch {  // 从 channel 取任务，天然串行
        ...渲染、打印...
    }
}
```

**知识点**：
- **goroutine**：Go 的轻量级线程，`go q.worker()` 一行启动后台循环。
- **channel 即队列**：`q.ch <- t` 入队、`for t := range q.ch` 出队。channel 本身线程安全，入队与消费天然同步。
- **单 worker 串行**：只有 1 个 worker 消费 channel，因此**同一时刻只处理一个任务**，从设计上避免了多任务并发打印时的资源竞争——这是 D2 的决策（先跑通主路径，串行瓶颈后续再优化）。
- **读写锁 `sync.RWMutex`**：`Submit`/`update` 用写锁 `Lock()`，`Get`（查询状态）用读锁 `RLock()`。HTTP 查询与 worker 更新状态是两个 goroutine，必须加锁保护 `map`，否则并发读写 map 会 panic（Go map 非线程安全）。
- **状态机**：`pending → completed / failed`，失败时记录 `Message`（原因）与 `OutputPath`（已产出的 PDF 路径）。

## 3. HTTP 服务与 REST 设计

代码位置：`internal/server/server.go`

- **`http.NewServeMux()` + 路由模式**：Go 1.22 起 mux 支持路径参数，`"/api/v1/tasks/{task_id}"` 用 `r.PathValue("task_id")` 取值。
- **资源化 REST**（D3 定案方案 B）：任务即资源 `/api/v1/tasks`；`POST` 创建、`GET /{id}` 查询；`healthz` 探活。
- **JSON 处理**：
  - 请求体 `{type, data}`，其中 `data` 用 `json.RawMessage` 先不解析，**拿到 type 后再按类型二次反序列化**（ticket → `ticketPayload`，code → `codePayload`）。这就是"data 按 type 分组"的实现方式，避免一个大 struct 里塞满无关字段。
  - 响应统一：成功 `writeJSON`，失败 `writeError` 输出 `{"error":{"code","message"}}`。
- **HTTP 状态码**：创建返回 `202 Accepted`（已入队），非法参数 `400`，不存在 `404`，方法不对 `405`，与契约错误码一一对应。

## 4. 中间件：装饰器模式

代码位置：`server.go` 的 `cors`、`LogMiddleware`

```go
return http.ListenAndServe(s.addr, LogMiddleware(cors(mux)))
```

**知识点**：HTTP 中间件 = 一个"包装 Handler 的 Handler"。`cors` 先设置跨域响应头、拦截 OPTIONS 预检；`LogMiddleware` 记录每次请求方法/路径/耗时。两者按声明顺序层层包裹最终 mux，实现**横切关注点**（日志、跨域）与业务路由解耦。这是 Web 框架（gin/echo）的核心思想，这里用标准库手写实现，无任何第三方依赖。

**CORS 背景**：前端页面由本服务托管（同源），但为了兼容"从别的页面跨域调用 API"的场景，仍加了 `Access-Control-Allow-Origin: *`。预检请求 `OPTIONS` 直接返回 `204`。

## 5. 静态资源内嵌：go:embed

代码位置：`internal/static/embed.go` + `index.html`

```go
//go:embed index.html
var IndexHTML string
```

**知识点**：`go:embed` 是 Go 1.16+ 的编译期资源内嵌机制。`index.html` 被**编译进二进制**，运行时 `static.IndexHTML` 直接可用，不依赖工作目录或磁盘上的 html 文件。**D4 曾踩坑**：早期版本从磁盘读 html，换个目录启动就 404；改为 embed 后从任何目录启动都能正常出页面——本次复现验证再次证明该修复有效。

## 6. 模板与 XSS 安全：html/template 自动转义

代码位置：`internal/render/render.go` 的 `ticketHTML` / `codeHTML`

```go
const tpl = `...<pre>{{.Source}}</pre>...`
t, _ := template.New("code").Parse(tpl)
t.Execute(&buf, map[string]string{"Source": source, "Lang": lang})
```

**知识点**：
- **`html/template` 自动上下文转义**：模板变量插值时，库会根据 HTML 上下文自动转义特殊字符。源码里的 `&&` 会被写成 `&amp;&amp;`，`<` 写成 `&lt;`，防止把用户提交的代码当成 HTML 标签执行（防 XSS）。
- **只转义一次的保证**：`html/template` 在整个模板渲染中只转义一次。如果先把源码手动 `html.EscapeString` 再塞进模板，就会**双重转义**（`&&` → `&amp;amp;&amp;amp;`），PDF 里显示乱码。D4 新增的 `render_test.go` 就是回归测试这一点。
- **字符串拼接 vs 模板**：ticket/code HTML 用 `html/template` 生成而非 `fmt.Sprintf` 拼字符串，核心原因就是上面的自动转义保障。

## 7. 无头浏览器渲染：Chrome headless → PDF

代码位置：`render.go` 的 `renderHTML`

```go
cmd := exec.CommandContext(ctx, e.chromePath,
    "--headless", "--disable-gpu", "--no-sandbox",
    fmt.Sprintf("--print-to-pdf=%s", pdfPath),
    "--virtual-time-budget=5000", "file:///...html")
```

**知识点**：
- **Chrome headless 模式**：无界面运行，`--print-to-pdf` 直接把 HTML 打印成 PDF，等效于浏览器"打印为 PDF"，因此**天然支持 CSS 尺寸（58mm/80mm 窄纸）与样式**。
- **临时文件**：先生成唯一命名的临时 HTML（纳秒时间戳防并发冲突），渲染完删除。
- **`exec.CommandContext` + 30 秒超时**：防止 Chrome 僵死导致 worker 永久阻塞——这是"渲染失败"而非"永久卡住"的兜底保障。
- **平台探测**：`findChrome()` 按操作系统（`runtime.GOOS`）探测常见路径（Win: Program Files；Linux: /usr/bin），再回退 `exec.LookPath`，最后兜底 `chrome` 命令名。

## 8. 跨平台打印：系统命令适配

代码位置：`internal/printer/printer.go`

```go
if runtime.GOOS == "windows" {
    cmd := exec.CommandContext(ctx, "powershell", "-Command",
        fmt.Sprintf("Start-Process -FilePath '%s' -Verb Print -Wait", pdfPath))
} else {
    cmd := exec.CommandContext(ctx, "lp", pdfPath)
}
```

**知识点**：
- **运行时探测 `runtime.GOOS`**：Windows 用 PowerShell 的 `Start-Process -Verb Print` 调默认打印机；Linux 用 CUPS 的 `lp` 命令。一份代码两套分支。
- **打印失败 = PDF 兜底，不视为失败**：`Print` 返回 `nil`（成功）即使实体打印失败，因为 PDF 本身已是产物。这是"无打印机环境"下的临时假实现（D7 计划借实机验证出纸）。
- **10 秒超时**：`-Wait` 在无打印机时可能挂起，超时保护避免阻塞 worker 队列。

## 9. 端口协商：net.Listen 探测

代码位置：`internal/port/port.go`

```go
for p := start; p <= end; p++ {
    addr := fmt.Sprintf("127.0.0.1:%d", p)
    ln, err := net.Listen("tcp", addr)
    if err == nil { ln.Close(); ...写入port.txt...; return addr }
}
```

**知识点**：`net.Listen` 成功 = 端口可用，立即关闭再交给后续 `http.ListenAndServe` 使用（存在极小的竞态窗口，但本场景可接受）。18210~18220 区间逐个尝试，实际地址写入 `port.txt` 供前端/脚本读取。**已知边界**：区间全被占用时 `panic` 退出（G3 差距项，计划优雅降级）。

## 10. 测试：回归测试保障关键行为

代码位置：`internal/render/render_test.go`

```go
func TestCodeEscape(t *testing.T) { ... }  // 转义只做一次
```

**知识点**：单元测试只测"最容易回归坏"的行为——HTML 转义。`go test ./...` 全绿即可放心重构。测试驱动了 D4 的"只转义一次"修复，是保证后续改动的安全网。

## 11. 知识点速查表

| 技术 | 用在哪 | 一句话作用 |
|---|---|---|
| goroutine | task.Start | 后台并发处理 |
| channel | task.Queue | 任务队列 + 串行消费 |
| sync.RWMutex | task.Queue | 并发安全的任务表 |
| http.NewServeMux + PathValue | server | Go 1.22 路由与路径参数 |
| json.RawMessage | server | 延迟解析 data，按 type 分组 |
| 中间件（Handler 包裹） | server | CORS、日志横切 |
| go:embed | static | HTML 内嵌进二进制 |
| html/template | render | 自动转义防 XSS |
| exec.CommandContext | render/printer | 调 Chrome/打印机 + 超时 |
| runtime.GOOS | render/printer | 跨平台分支 |
| net.Listen 探测 | port | 端口协商回退 |
| go test | render_test | 转义回归保障 |

## 12. 若想深入学习

- 并发：读 Go 官方《Effective Go》的 Channels 章节；看 `task.go` 的 worker 循环。
- 模板安全：Go 官方博客《Go Templates》与 `html/template` 文档。
- REST：对比本项目契约 `接口书面约定_20260821.md` 与 `server.go` 实现。
- 无头渲染：Chrome DevTools 文档 `--headless=new` 与 `--print-to-pdf` 参数。
