# CCPC 本地打印服务 HTTP 接口书面约定（v1.0）

> 约定状态：**已冻结**。本约定记录"浏览器调用本地打印机——基于 Golang 的跨平台本地打印服务（MVP）"的 HTTP 接口契约，作为后续模块开发与联调的基准。
>
> - 方案编号：方案 B（REST 资源化 + 按类型分组）
> - 约定日期：2026-08-21（第 3 天）
> - 定案人：侯俊（立项人）
> - 适用范围：`printsvc` 服务端与 Web 前端、以及任何第三方打印调用方

---

## 1. 总体约定

| 项目 | 约定值 |
| --- | --- |
| 服务地址 | `http://127.0.0.1:{port}`，端口由 `port.txt` / 启动日志决定（默认 18210，被占用时在 18210~18220 内递增回退） |
| API 版本前缀 | `/api/v1` |
| 编码 | 请求与响应均为 UTF-8 JSON，`Content-Type: application/json` |
| 字段命名 | JSON 字段一律 snake_case |
| 时间格式 | `created_at` 用 RFC3339（如 `2026-08-21T10:30:00+08:00`）；`pass_time` 用竞赛现场可读的 `YYYY-MM-DD HH:MM:SS` |
| 任务 ID 格式 | `T{4 位序号}-{unix 秒}`，如 `T0001-1787279400` |
| 错误格式 | 所有错误响应统一为 `{"error": {"code": "<ERR_CODE>", "message": "<可读信息>"}}` |
| CORS | 仅允许本地来源跨域探活（D8 安全强化 G7）：`Origin` ∈ `127.0.0.1`/`::1`/`localhost` 时回 `Access-Control-Allow-Origin`，其余来源不设 CORS 头 |

### 错误码表

| code | HTTP 状态 | 含义 |
| --- | --- | --- |
| `INVALID_ARGUMENT` | 400 | 请求体非法、字段缺失或类型错误 |
| `NOT_FOUND` | 404 | 任务 ID 不存在 |
| `METHOD_NOT_ALLOWED` | 405 | 方法不允许 |
| `RENDER_FAILED` | 200（任务状态=failed） | 后台渲染失败，任务置为 failed 并带 message |
| `PRINT_FAILED` | 200（任务状态=failed） | 打印失败（无实体打印机时 PDF 兜底，不视为失败） |

> 说明：`RENDER_FAILED` / `PRINT_FAILED` 属于**异步失败**，通过查询接口的任务 `status=failed` 与 `message` 呈现，不占用同步响应码。

### 任务状态机

```
pending ──渲染成功 + 打印/PDF兜底成功──> completed
   │
   └────渲染失败或打印硬失败───────> failed
```

---

## 2. 接口清单

### 2.1 探活 `GET /api/v1/healthz`

**成功一例（HTTP 200）：**

请求：`GET http://127.0.0.1:18210/api/v1/healthz`

```json
{
  "service": "ccpc-printsvc",
  "status": "ok",
  "version": "v0.2.0"
}
```

### 2.2 提交打印任务 `POST /api/v1/tasks`

**请求体（按 `type` 分组）：**

气球小票（type=`ticket`）：

```json
{
  "type": "ticket",
  "data": {
    "team": "Team A",
    "problem": "A",
    "pass_time": "2026-08-20 10:23:45"
  }
}
```

代码打印（type=`code`）：

```json
{
  "type": "code",
  "data": {
    "lang": "cpp",
    "source": "#include <bits/stdc++.h>\nusing namespace std;\nint main() { return 0; }"
  }
}
```

**成功一例（HTTP 202 Accepted）：**

```json
{
  "task_id": "T0001-1787279400",
  "status": "pending",
  "created_at": "2026-08-21T10:30:00+08:00"
}
```

**失败一例（HTTP 400，`type` 非法）：**

请求：

```json
{
  "type": "pdf",
  "data": {}
}
```

响应：

```json
{
  "error": {
    "code": "INVALID_ARGUMENT",
    "message": "type must be ticket or code"
  }
}
```

**失败一例（HTTP 400，ticket 缺 `team`）：**

```json
{
  "error": {
    "code": "INVALID_ARGUMENT",
    "message": "data.team required for ticket"
  }
}
```

字段校验规则：

| type | 必填字段 | 说明 |
| --- | --- | --- |
| `ticket` | `data.team`、`data.problem` | `data.pass_time` 可选，为空时渲染为 `--` |
| `code` | `data.source` | `data.lang` 可选，为空时渲染为 `text` |

### 2.3 查询任务 `GET /api/v1/tasks/{task_id}`

**成功一例（HTTP 200，任务已完成，ticket）：**

请求：`GET http://127.0.0.1:18210/api/v1/tasks/T0001-1787279400`

```json
{
  "task_id": "T0001-1787279400",
  "type": "ticket",
  "status": "completed",
  "output_path": "E:\\CodeBuddyConfig\\20260819144818\\printsvc\\output\\ticket_1787279400000000000.pdf",
  "created_at": "2026-08-21T10:30:00+08:00",
  "data": {
    "team": "Team A",
    "problem": "A",
    "pass_time": "2026-08-20 10:23:45"
  }
}
```

**成功一例（HTTP 200，任务排队中）：**

```json
{
  "task_id": "T0001-1787279400",
  "type": "ticket",
  "status": "pending",
  "created_at": "2026-08-21T10:30:00+08:00",
  "data": {
    "team": "Team A",
    "problem": "A",
    "pass_time": "2026-08-20 10:23:45"
  }
}
```

**失败一例（HTTP 404，任务不存在）：**

请求：`GET http://127.0.0.1:18210/api/v1/tasks/T9999-1`

```json
{
  "error": {
    "code": "NOT_FOUND",
    "message": "task not found: T9999-1"
  }
}
```

---

## 3. 与旧接口的对应关系（迁移记录）

| 旧路由（v0.1，D2 骨架） | 新路由（v1.0，本约定） | 说明 |
| --- | --- | --- |
| `GET /healthz` | `GET /api/v1/healthz` | 前缀化 |
| `POST /submit`（扁平字段） | `POST /api/v1/tasks`（`type`+`data` 分组） | 请求体结构变更 |
| `GET /status?id=xxx` | `GET /api/v1/tasks/{id}` | 查询参数改为路径参数 |
| 错误 `{"error":"..."}` | 统一 `{"error":{"code":...,"message":...}}` | 结构化错误 |

> 旧路由 `POST /submit`、`GET /status?id=` 于 v1.0 起**废弃**，不再提供兼容转发（决策过程见第 3 天报告"问题、计划与 AI 沟通"）。

---

## 4. 实现落点（代码位置）

| 约定项 | 代码位置 |
| --- | --- |
| 路由注册 | `internal/server/server.go` |
| 任务实体 / 状态机 | `internal/task/task.go`（`Task`、`Status`、`Queue`） |
| 字段分组校验 | `internal/server/server.go`（`handleCreateTask`） |
| 渲染分派 | `internal/task/task.go`（`worker` 按 `Type` 分派 `RenderTicket` / `RenderCode`） |
| 前端调用 | `internal/static/index.html`（`API_BASE` 切换为 `/api/v1`） |

## 5. 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-08-21 | 冻结方案 B 约定；含成功/失败示例、错误码、状态机 |
| v1.0（勘误） | 2026-08-26 | 安全强化（D8 G7）：CORS `*` 改为本机来源白名单。接口路径/字段/错误码/状态机均不变，不属契约语义变更，定案人：侯俊（立项人） |
