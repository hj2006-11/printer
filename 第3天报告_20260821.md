# 课程项目第 3 天报告

## 基本信息

- 课题编号：CCP-2026-001
- 课题名称：浏览器调用本地打印机——基于 Golang 的跨平台本地打印服务（MVP）
- 开发方式：独立完成
- 成员 / 立项人：侯俊
- 报告日期：2026-08-21（第 3 天）
- 工期安排：共 9 天，2026-08-19 至 2026-08-27

## 今日目标

第 2 天报告明确了四个待决事项，其中"D3 冻结 HTTP API 字段"是今天的关键约定。今天的目标是把"接口长什么样"彻底写死，并让代码与约定一一对应：

1. **接口方案定案**：就 HTTP 接口给出多个候选方案并比选，由立项人拍板最终方案（方案 B：REST 资源化 + 按类型分组）；
2. **输出书面接口约定**：把定案方案写成可复核的约定文档，含成功一例、失败一例、错误码表与状态机；
3. **明确主路径数据对象**：以数据模型方式表达任务实体、状态机、队列与数据流，与接口约定逐条核对一致；
4. **代码按契约改造**：把 D2 骨架中的扁平 `/submit`、`/status?id=` 迁移为 `/api/v1/tasks` 资源路由，前端同步切换，构建并冒烟验证；
5. **建立项目备忘**：把技术栈、约定索引、待决问题沉淀到备忘文件，供后续 AI 与本人快速读取。

## 主路径上的数据或对象

主路径"提交→排队→渲染→打印→查询"上只依赖 4 类数据对象，其余均不涉及：

| 对象 | 形态 | 用途 | 关联约定 |
| --- | --- | --- | --- |
| `Task`（打印任务） | 内存结构体，兼作 JSON 载体 | 主路径核心实体，贯穿提交到查询 | `task_id`、`type`、`status`、`data`（按类型分组） |
| `Queue`（任务队列） | 有缓冲 channel + 索引 map + 读写锁 | 串行消费任务，保证打印不争用；供查询 | 状态机 pending/completed/failed |
| PDF 产物 | `output/{type}_{纳秒}.pdf` | 打印输入；无实体打印机时的最终产物 | `output_path` 字段 |
| `port.txt` | `127.0.0.1:{port}` | 记录实际监听端口，供页面与人工核对 | 端口协商 18210~18220 |

其中 `Task.data` 是关键分组字段：`ticket` 载荷为 `{team, problem, pass_time}`，`code` 载荷为 `{lang, source}`，校验规则按类型分开执行，避免字段混放导致的口径不清。

## 调用约定

就主路径最关键的一步——**提交任务**——按定案方案 B 冻结如下（完整约定见《接口书面约定 v1.0》与《数据模型 v1.0》）：

### 成功一例：提交气球小票任务

请求 `POST http://127.0.0.1:18210/api/v1/tasks`：

```json
{
  "type": "ticket",
  "data": {
    "team": "Team A",
    "problem": "A",
    "pass_time": "2026-08-20 10:23:45"
  }
}
```

响应 `202 Accepted`：

```json
{
  "task_id": "T0001-1755718800",
  "status": "pending",
  "created_at": "2026-08-21T10:30:00+08:00"
}
```

随后 `GET /api/v1/tasks/T0001-1755718800` 返回 `status: "completed"`、`output_path` 指向 PDF 产物，`data` 原样回传。

### 失败一例：`type` 非法 / 必填字段缺失

请求 `POST /api/v1/tasks`，`type` 为 `"pdf"`：

```json
{ "type": "pdf", "data": {} }
```

响应 `400 Bad Request`，统一错误结构：

```json
{
  "error": {
    "code": "INVALID_ARGUMENT",
    "message": "type must be ticket or code"
  }
}
```

若 `type: "ticket"` 但缺 `data.team`，同样返回 `400`，`message` 为 `data.team required for ticket`。

### 约定要点

- 所有错误统一为 `{"error": {"code": ..., "message": ...}}`，code 取 `INVALID_ARGUMENT` / `NOT_FOUND` / `METHOD_NOT_ALLOWED`；
- 时间格式：`created_at` 用 RFC3339，`pass_time` 用现场可读的 `YYYY-MM-DD HH:MM:SS`；
- 任务 ID 格式 `T{4 位序号}-{unix 秒}`；状态机 `pending → completed | failed`，`RENDER_FAILED` / `PRINT_FAILED` 为异步失败，体现在任务状态而非同步响应码。

## 临时假实现

以下环节今天仍用假实现或兜底，必须标明，避免被当成已完成：

| 环节 | 现状（D3） | 换真计划 |
| --- | --- | --- |
| 语法高亮 | 用 HTML 转义 + CSS 着色模拟关键字高亮（chroma 未接入） | D4 拉取 chroma 并替换，离线则保留现有转义 |
| 实体打印 | 无实体打印机，走"打印到 PDF"兜底 | D7 借实机验证出纸；若不可得，以 PDF 产物作为验收依据 |
| Linux 渲染 | 未在本机运行（本机无 WSL），Windows 已端到端验证 | D6 交叉编译产物 + 远程 Linux 渲染命令截图存档 |
| 端口协商 | 已实现 18210~18220 回退；"区间全被占用"未实测 | D5 用脚本占满区间验证最终行为（报错退出） |

## 项目备忘要点

备忘已建立（见 `03_项目备忘_20260821.md`），索引如下：

- 技术栈：Golang 1.2x + 无头 Chromium + 系统默认打印（PowerShell `-Verb Print` / `lp`），进程内 HTTP 服务；
- 约定索引：接口 v1.0 冻结于《接口书面约定_20260821.md》，数据模型 v1.0 冻结于《数据模型_20260821.md》；
- 关键字段：`task_id` 格式、`data` 按 `type` 分组、错误三码、`created_at`/`pass_time` 时间格式；
- 待决问题：语法高亮换真（D4）、Linux 环境定案（D6 前）、端口全占用边界（D5）。

## 今日完成与自检

### 今天实际完成的内容

1. 接口方案比选：给出 A（扁平）、B（REST 分组）、C（统一封装）、D（兼容转发）四个候选，立项人定案 **方案 B**；
2. 输出《接口书面约定 v1.0》：含路由、请求/响应示例、错误码表、状态机、旧路由迁移对照，并记录 AI 沟通中"契约如何裁剪"的要点；
3. 输出《数据模型 v1.0》：Task 实体、状态机图、Queue 结构、数据流图，与接口约定 6 项一致性核对全部通过；
4. 代码按契约改造：服务端迁移到 `/api/v1/tasks` 资源路由（`POST` 提交 / `GET /api/v1/tasks/{id}` 查询），新增按 `type` 分组的字段校验与结构化错误；前端切到 `/api/v1` 基路径；构建通过并冒烟验证（探活 / 提交小票 / 轮询状态 / 失败用例）；
5. 建立 `03_项目备忘`：摘抄技术栈、约定索引、待决问题。

### 自检对照

- 约定是否齐备：是——提交、查询、探活、错误格式、状态机均有成功与失败示例；
- 假实现边界是否都已标明：是——语法高亮、实体打印、Linux 渲染、端口全占用 4 项均标注现状与换真日期。

## 问题、计划与 AI 沟通

### 不成功的沟通

1. 首次请 AI 直接生成"接口约定文档"时，AI 产出了覆盖列表页、删除、取消等十余个接口的完整方案，与 9 天 MVP 主路径严重不符。改进：改请 AI 只写"提交与查询两个接口 + 健康检查"，并强制附带成功/失败示例，产出才收敛到可用。
2. 让 AI 直接改代码时，AI 在未确认接口方案前先写了兼容层（新旧路由并存）。改进：先冻结方案再动代码，避免返工。

### 比较有效的沟通

1. 明确指令"给出多套接口方案（含优劣势），由我拍板后再写文档"，AI 一次给出 4 个可对比方案，人工定案高效。
2. 要求 AI"数据模型只列主路径对象，并逐条与接口约定核对"，产出被控制在 4 张表 + 1 张数据流图，没有膨胀成完整库设计。
3. 代码改造前先通读 D1/D2 报告与现有骨架，迁移清单（旧路由→新路由→前端→文档）一次对齐。

### 明日安排（D4）

1. **语法高亮换真**：接入 chroma 高亮库替换模拟着色，渲染代码打印样张核对折行与关键字色；
2. **任务队列完善**：验证多任务串行排队（提交 3 个任务观察顺序与状态流转），补齐 `Queue` 边界测试；
3. **前端提交/状态页美化**：按接口 v1.0 调整页面表单与轮询逻辑，展示任务列表与失败原因；
4. **Linux 依赖清单**：整理 chromium、fonts-noto-cjk、cups 安装命令，为 D6 交叉编译验证做准备。

---

**附件：**
- `接口书面约定_20260821.md` / `.docx`：HTTP 接口书面约定 v1.0
- `数据模型_20260821.md` / `.docx`：数据模型 v1.0
- `03_项目备忘_20260821.md`：项目备忘
- `printsvc/`：按契约 v1.0 改造后的代码
