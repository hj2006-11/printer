# 课程项目第 4 天报告

## 基本信息

- 课题编号：CCP-2026-001
- 课题名称：浏览器调用本地打印机——基于 Golang 的跨平台本地打印服务（MVP）
- 开发方式：独立完成
- 成员 / 立项人：侯俊
- 报告日期：2026-08-22（第 4 天）
- 工期安排：共 9 天，2026-08-19 至 2026-08-27

## 今日目标

第 3 天报告冻结了接口 v1.0 与数据模型，代码已完成契约化改造。今天进入课程"工程起步"阶段，目标是让项目从"本人能跑"提升为"他人按文字也能跑起来"，并留下可追溯的 Git 痕迹：

1. **可跟做启动**：输出仓库根 README，覆盖依赖安装、启动、停止、验证四个环节，命令逐条实测；
2. **目录职责明确**：按职责整理目录结构，报告能指明"改哪里"；
3. **人工审查**：对 D1~D3 生成的初始代码做带检查项的审查，发现的问题修掉或写明保留原因；
4. **可追溯提交**：整理 .gitignore（逐条说明忽略原因），完成 ≥3 次小而精提交，每次对应一件具体任务；
5. **仓库收敛**：将前 3 天报告、数据模型、项目备忘移出仓库（本机保留），仓库只留代码、接口契约与当轮报告。

## 如何启动

以下步骤 2026-08-22 在本机（Windows 11）逐条实测通过。

### 依赖安装

| 依赖 | 版本 | 检查命令 | 说明 |
| --- | --- | --- | --- |
| Go | 1.22+（开发用 1.26） | `go version` | 官网安装后加入 PATH |
| Chrome / Chromium | 近期版本即可 | 见 README | 渲染 HTML→PDF；Windows 探测 `Program Files` 下 `chrome.exe`，Linux 探测 PATH 中 `chromium`/`google-chrome` |

### 启动

```bash
cd printsvc
go build -o printsvc.exe ./cmd/printsvc
./printsvc.exe
```

启动日志打印实际监听地址（默认 `http://127.0.0.1:18210`），同时写入 `port.txt`；端口被占用时自动在 18210~18220 内回退。浏览器打开该地址即进入提交页。

### 停止

- 前台运行：运行窗口按 `Ctrl+C`；
- 后台运行（Windows PowerShell）：`Stop-Process -Name printsvc`。

### 验证

```powershell
curl.exe http://127.0.0.1:18210/api/v1/healthz          # 1 探活
curl.exe -X POST http://127.0.0.1:18210/api/v1/tasks -H "Content-Type: application/json" -d "{\"type\":\"ticket\",\"data\":{\"team\":\"Alpha\",\"problem\":\"A+B\",\"pass_time\":\"2026-08-22 10:00:00\"}}"   # 2 提交小票任务
curl.exe http://127.0.0.1:18210/api/v1/tasks/<task_id>  # 3 查询状态
```

任务完成后 `printsvc/output/` 下出现 PDF 产物，响应中的 `output_path` 即绝对路径。今日实测：从临时目录启动 `printsvc.exe`（验证静态资源已内嵌、不依赖工作目录），提交含 `<`、`>`、`&&` 的代码任务，状态 `pending → completed`，PDF 正常生成。

## 目录或工程结构

| 路径 | 职责 | 修改入口 |
| --- | --- | --- |
| `printsvc/cmd/printsvc/main.go` | 装配：端口协商、模块注入、启动 HTTP | 启动参数、日志 |
| `printsvc/internal/server` | HTTP API v1.0 路由 + 静态页（go:embed） | 增删接口、改路由 |
| `printsvc/internal/task` | 任务队列（单 worker 串行）+ 状态机 | 排队策略、失败分类 |
| `printsvc/internal/render` | Chrome 无头渲染 HTML→PDF（58/80mm） | 版式、语法高亮 |
| `printsvc/internal/printer` | 系统默认打印，PDF 兜底 | 驱动适配 |
| `printsvc/internal/port` | 端口协商 18210~18220 | 端口策略 |
| `printsvc/internal/static` | 前端页面（内嵌进二进制） | 表单、轮询 |
| `scripts/` | 文档转换/校验/提交脚本 | 文档生成 |
| `README.md`、`接口书面约定_20260821.md` | 启动说明、接口契约 v1.0 | 文档维护 |

## 审查纪要

- 审查对象：D1~D3 生成的 `printsvc` 初始代码（server / render / task / printer / port / main）；
- 审查人：立项人（侯俊）；审查时间：2026-08-22；
- 检查项：接口契约一致性、资源路径健壮性、HTML 转义正确性、超时与错误处理、可跟做性。

发现并处理的问题：

| # | 问题 | 所属检查项 | 结论 |
| --- | --- | --- | --- |
| 1 | 静态页用相对路径 `internal/static`，换工作目录启动 → 页面 404 | 资源路径健壮性 | **已修**：`go:embed` 将页面内嵌进二进制 |
| 2 | 代码打印双重 HTML 转义（`HTMLEscapeString` 预转义 + html/template 再转义），`<` 显示成 `&lt;` 字面量 | HTML 转义正确性 | **已修**：交 html/template 自动转义，新增回归测试 |
| 3 | 遗留 `test_api.py` 仍调 v0 的 `/submit`、`/status?id=`，与契约 v1.0 不符 | 接口契约一致性 | **已删**：避免误导后续维护 |
| 4 | 无实体打印机时打印阶段阻塞至 10s 超时后走 PDF 兜底 | 超时与错误处理 | **保留**：PDF 兜底即设计目标，实机验证安排在 D7 |

审查结论：三项代码缺陷已全部修复并有测试/实测佐证；第四项为已知设计行为，写明原因后保留。

## 协作与提交

今日共 3 次提交，小步提交、每次对应一件具体任务，可独立回溯：

| 提交 | 内容 | 涉及文件 |
| --- | --- | --- |
| ① docs | README 启动说明 + .gitignore 逐条原因；历史报告/数据模型/项目备忘移出仓库 | `README.md`、`.gitignore` |
| ② fix | 静态资源 embed、双重转义修复 + 回归测试、删除过期 test_api.py | `server.go`、`render.go`、`embed.go`、`render_test.go` |
| ③ docs | 第 4 天报告 md + docx | `第4天报告_20260822.md`/`.docx` |

`git diff` 对照（提交 ② 前后）：`server` 静态目录由"读文件"改为"读内嵌字符串"；`render` 源码字段由"预转义结果"改为"原始源码"（转义交给模板引擎）；新增 `embed.go` 与 `render_test.go`；删除 `test_api.py`。

## 今日完成与自检

### 今天实际完成的内容

1. 实测并记录可跟做的安装 / 启动 / 停止 / 验证步骤，含换目录启动与特殊字符代码任务的全链路验证；
2. 编写仓库根 `README.md`（目录职责、接口 v1.0 摘要、.gitignore 说明）；
3. 完成一次带检查项的人工审查，修复 3 个真实缺陷并补回归测试；
4. 重写 `.gitignore`，按"构建产物 / 运行产物 / 缓存 / 临时文件 / 历史文档 / 系统垃圾"逐条写明忽略原因；
5. 将前 3 天报告、数据模型、项目备忘移出仓库（本机保留），仓库收敛为"代码 + 契约 + 当轮报告"；
6. 完成 3 次小而精提交并推送。

### 自检对照

- 启动步骤能否跟做：是——每条命令本机实测，换目录启动已验证；
- 审查是否有检查项与结论：是——5 项检查、4 个发现、3 修 1 保留，均有依据；
- 目录职责是否清楚：是——表格逐目录给出"改哪里"；
- 提交是否 ≥3 次且小而精：是——docs / fix / docs，各对应一件任务；
- .gitignore 是否逐条说明原因：是。

## 问题、计划与 AI 沟通

### 问题与已知限制

1. 本机无实体打印机，打印走 PDF 兜底（D7 借实机验证出纸）；
2. 语法高亮仍是"转义 + CSS 模拟"，chroma 未接入（离线环境暂不引入依赖）；
3. Linux 渲染未真机验证（本机无 WSL），计划 D6 交叉编译 + 远程验证。

### 与 AI 沟通记录（比较有效）

1. 要求 AI"先通读前三天报告与代码再动手"，今日任务清单一次对齐，没有返工；
2. 动手前把"删除范围 / 今日范围 / 提交分界"三个分歧点交立项人拍板，AI 未擅自删改任何文件；
3. 要求"内容必须有真实依据"，AI 先构建、跑端到端验证、再写报告，审查纪要全部来自实测。

### 明日安排（D5）

1. 前端提交/状态页美化与实时队列展示；
2. Queue 多任务串行边界测试；
3. 端口全占用（18210~18220）边界验证；
4. 语法高亮换真（chroma）与代码打印样张核对。

---

**附件：**
- `README.md`：工程起步说明（安装/启动/停止/验证）
- `第4天报告_20260822.md` / `.docx`：本报告
- `printsvc/`：审查修复后的代码
